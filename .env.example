# Copy to .env and fill in. .env is git-ignored.

# Ethereum mainnet JSON-RPC endpoint (including any API key).
# Used only by script/l1_price.sh to read base fees with eth_feeHistory.
ETH_MAINNET_RPC_URL=
